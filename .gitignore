.DS_Store
*.swp
*~
node_modules/

# Tool state that should never ship to students
.impeccable/
.playwright-mcp/

# Course skills are project content. The global ignore excludes .claude/, so
# both levels must be re-included or Git silently omits every slash command.
!.claude/
!.claude/**

# Final case-study PDF export lives in Google Drive / Behance, not git (exceeds GitHub 100MB limit)
projects/arogga-redesign/arogga case study.pdf
